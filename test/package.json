{
  "name": "scally-test",
  "version": "0.0.0",
  "description": "Test suite for Scally",
  "scripts": {
    "compile-scally-src": "node-sass --output-style expanded -o compile-scally ../src/scally.scss",
    "compile-scally-settings": "node-sass --output-style expanded -o compile-scally compile-scally/settings.scss"
  }
}
